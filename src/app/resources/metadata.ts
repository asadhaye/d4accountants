import { createPageMetadata } from '@/lib/metadata';

export const metadata = createPageMetadata(
  'Resources | D4 Accountants',
  'Expert guides and tools to support your financial journey, including tax guides, templates, and business insights.',
  '/resources'
);