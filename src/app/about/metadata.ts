import { createPageMetadata } from '@/lib/metadata';

export const metadata = createPageMetadata(
  'About Us | D4 Accountants',
  'Learn about D4 Accountants, our mission, values, and the dedicated team behind our accounting services.',
  '/about'
);