import { createPageMetadata, defaultViewport } from '@/lib/metadata';

export const metadata = createPageMetadata(
  'Offline | D4 Accountants',
  'You are currently offline',
  '/offline'
);

export const viewport = defaultViewport;